{
  "name": "axistream",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm -ws --if-present run test",
    "dev": "npm -w @axistream/app run dev",
    "build": "npm -ws --if-present run build"
  }
}
